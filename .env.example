# Optional overrides. The app loads ".env" automatically at startup.
# Leave a value blank to use the repository default or system PATH.

WAV_PYTHON=
WAV_VENV_DIR=.venv
WAV_SETTINGS_PATH=runtime_settings.yaml
WAV_RUNTIME_ROOT=runtime/softvc_clean
WAV_DATA_ROOT=workspace_data
WAV_JOBS_DIR=jobs
WAV_TRAINED_MODELS_DIR=trained_models
WAV_SEPARATORS_DIR=runtime/separators
WAV_SEPARATOR_MODELS_DIR=runtime/separators/models
WAV_SEPARATOR_CACHE_DIR=runtime/separators/cache
WAV_SEPARATOR_EXE=
WAV_FFMPEG_EXE=
