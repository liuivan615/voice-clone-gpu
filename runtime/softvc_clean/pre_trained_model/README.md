# Base Model Placeholder

This directory is intentionally empty in the GitHub export.

Add the base generator/discriminator checkpoints and optional diffusion base models under this tree.

Expected subpaths include:

- `768l12/`
- `hubertsoft/`
- `whisper-ppg/`
- `tiny/vec768l12_vol_emb/`
- `diffusion/`

See `docs/model_setup.md` for the expected filenames.
