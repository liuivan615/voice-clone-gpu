# ffmpeg Placeholder

No ffmpeg binaries are committed to this repository.

Use one of the following approaches:

- Install `ffmpeg` globally and make it available in `PATH`
- Set `WAV_FFMPEG_EXE` in `.env`
- Place your local ffmpeg files in this directory if you want a repo-relative setup

See `docs/model_setup.md` for more context.
